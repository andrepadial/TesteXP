@model IEnumerable<Mvc_App_Crud.Models.Assunto>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Novo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Assunto1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Assunto1)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.AssuntoID }) |
            @Html.ActionLink("Visualizar", "Details", new { id=item.AssuntoID }) |
            @Html.ActionLink("Excluir", "Delete", new { id = item.AssuntoID }, new { @onClick = "javascript:return " + "confirm('Confirma a exclusão ?')" })
        </td>
    </tr>
}

</table>
